eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/

spring:
  application:
    name: Config-Client

  cloud:
    config:
     uri: http://localhost:8300
     fail-fast: true

  profiles:
    active: dev
